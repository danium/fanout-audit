Head query (inferred): "What is a quarnet?" — one sentence, purely definitional: a reader lands on this page to find out what an obsolete unit of dry volume means.

Sub-query generation checked against the nine intent classes:

- Definitional — OPEN. A reader plausibly arrives never having heard the term.
- Mechanism — closed. A unit conversion isn't a system with a "how/why does it work"; there's no mechanism to ask after.
- Comparison — closed. Nothing about an obsolete measure is weighed against alternatives; no one chooses a quarnet over a bushel today.
- Qualification — closed. Not something a reader adopts, buys, or follows.
- Constraint — closed. A definition, not a system with limits, requirements, or a price.
- Evidence — closed. No outcome is claimed, by the subject or by the page, that a reader would want proof of.
- Procedural — closed. Nothing to execute or set up.
- Recency — closed. The unit's obsolescence is already a settled historical fact, not an ongoing state a reader must check.
- Verification — closed. No dated or datable event is asserted whose occurrence a reader might doubt ("standardised on metric measures" names no date or occurrence).

Only one gate opens (Definitional). That is two or fewer, so per the audit's guardrail this stops here rather than producing a fan-out coverage report — a page with only one open gate signals the head query is too narrow, not that the content is thin.

Question for you: should the head query be broadened — e.g. to something like "historical units of dry grain measurement" or "archaic trade units and their modern equivalents" — so the audit has enough surface to run against? If you'd rather keep the scope exactly as-is (a single obsolete unit, defined for its own sake), say so and I'll confirm that's intentional before re-running, since a one-gate audit has nothing left to check.
